import React from 'react';
import SupplyModal from '../common/SupplyModal';

const LeverageSupplyModal = ({ onClose }: { onClose: () => void }) => {
  return <SupplyModal onClose={onClose} />;
};

export default LeverageSupplyModal;
